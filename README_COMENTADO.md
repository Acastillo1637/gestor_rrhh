# Gestor RRHH — versión comentada

Esta copia contiene comentarios explicativos agregados al código sin alterar la lógica principal.

## Correcciones realizadas
- Se mejoró el texto del `placeholder` del buscador en `personal/templates/listar.html`.
- Se agregó `aria-label` al campo de búsqueda para mejorar accesibilidad.
- Se eliminó una etiqueta `<body>` duplicada detectada en `listar.html`.

## Nota
Las credenciales de demostración del comando `cargar_datos` se mantienen porque forman parte de la lógica actual del proyecto. Para producción deberían trasladarse a variables de entorno y no quedar escritas directamente en el código.
